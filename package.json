{
  "name": "forum-diapason-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Scripts racine pour lancer Forum Diapason facilement",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && go run ./cmd/api\"",
    "dev:front": "cd frontend && npm run dev",
    "dev:back": "cd backend && go run ./cmd/api",
    "test": "cd frontend && npm test && cd ../backend && go test ./..."
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
